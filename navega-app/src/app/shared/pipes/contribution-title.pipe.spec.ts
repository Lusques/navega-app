import { ContributionTitlePipe } from './contribution-title.pipe';

describe('ContributionTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new ContributionTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
