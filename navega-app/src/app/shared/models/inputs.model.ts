export interface InputRadioCategorie {
  label: string;
  key: string;
}
export interface InputNumberConfig {
  symbol: string;
  inputId: string;
  title: string;
  local: string;
  minFractionDigits: number;
}
